[package]
name = "proef-fixture"
description = "Local fixture API server for the proef integration suite (sync, no async runtime)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# tiny_http, not axum: the workspace bans the tokio runtime (ADR-0006 +
# deny.toml); a synchronous fixture keeps the invariant binary-wide.
tiny_http = { workspace = true }

[lints]
workspace = true
